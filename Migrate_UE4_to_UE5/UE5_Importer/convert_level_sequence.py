import logging

from utility import re_package_name

import unreal


def build_level_sequence(migrate_tool, package_name, uobject, level_sequence):
    logging.error(f'not implemented build_level_sequence: {package_name}')
